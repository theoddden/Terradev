apiVersion: v1
kind: Service
metadata:
  name: vllm-lora
  namespace: lora-serving
  labels:
    app: vllm-lora
    app.kubernetes.io/name: vllm-lora
    app.kubernetes.io/component: inference
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vllm-lora
