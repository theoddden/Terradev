apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lora-serving.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lora-serving.labels" . | nindent 4 }}
